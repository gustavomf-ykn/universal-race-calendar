{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "apps/openresults-worker/Dockerfile.worker" },
  "deploy": {
    "startCommand": "python -m worker",
    "numReplicas": 1,
    "restartPolicyType": "ALWAYS"
  }
}
