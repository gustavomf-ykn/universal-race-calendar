# Contributing

Use branches pequenas, rode os checks locais e mantenha TicketSports isolado como fonte/adaptador.

```bash
pnpm lint
pnpm typecheck
pnpm test
```

Nao commite chaves de API. O projeto deve funcionar com `AI_PROVIDER=mock`.
